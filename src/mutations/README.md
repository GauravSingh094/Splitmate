# mutations/

Shared TanStack Query mutation hooks that are used across multiple features.
Feature-specific mutations belong in `src/features/<name>/mutations/`.

Only truly cross-cutting mutations (e.g. upload, notifications) live here.
