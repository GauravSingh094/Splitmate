# queries/

Shared TanStack Query hooks used across multiple features.
Feature-specific queries belong in `src/features/<name>/queries/`.

Only cross-cutting queries (e.g. current user, app config) live here.
