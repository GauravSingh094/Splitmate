# assets/

Static assets bundled by the Next.js compiler.

- `images/` — logo, illustrations, and other images (prefer SVG)
- `icons/` — custom SVG icon files (prefer Lucide React first)
- `fonts/` — any custom font files not available via next/font

Do not place large binary files here. Use the CDN / object store instead.
