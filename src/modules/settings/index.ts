/**
 * Settings Feature Module Layer.
 * Prepared for account settings, notification preferences, and security options.
 */
export const SETTINGS_MODULE = {
  id: 'settings',
  name: 'Settings',
} as const;
