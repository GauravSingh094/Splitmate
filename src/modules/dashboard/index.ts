/**
 * Dashboard Feature Module Layer.
 * Prepared for future dashboard widgets, analytics summary cards, and recent activity feeds.
 */
export const DASHBOARD_MODULE = {
  id: 'dashboard',
  name: 'Dashboard',
} as const;
